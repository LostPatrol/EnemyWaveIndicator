<!-- Test matrix and precise gaps for the requested native refinement. -->
# task1 验收与后续实现缺口

当前代码是 **0.9.0 虫潮类型测试候选**，已接入自然潮及当前EWC目录中35种具体虫潮控制器。表中的“已实现”指代码与所列离线测试，不等于新增类型实机验收通过。完整类型与边界见[WAVE-TYPES.md](WAVE-TYPES.md)。

| 任务要求 | 当前结果 |
|---|---|
| 恢复 DLL；MintCat 导入，无玩家脚本 | 已生成对应构建/打包流程，实际 MintCat 导入待测 |
| 透明度、实时 RGBA 预览 | 透明度默认 0.4；草稿颜色/透明度色块预览；不是 3D 光球预览 |
| 默认红白 2 Hz；颜色 A/B、频率配置 | 已实现并冷加载测试；2 Hz 是每秒两个完整 A/B 周期 |
| 精准自然潮归因 | 保留六个调度返回地址和逐请求归因；用户已确认0.8.0初始化修复后自然潮可显示；0.9.0需要实机回归 |
| 其他明确定义虫潮 | 35种具体EWC控制器已接入；独立Boss召唤、机器事件自有组件、Mod新增控制器等非目录来源未覆盖 |
| 分类型开关与文字 | 36项已接入，冷加载验证全部默认值、复选框/文字持久化、类型对应显示/标签；外观继续统一 |
| 默认仅自然潮 | 已实现；其他35项需主动启用并Apply |
| 重叠不同类型分别识别 | 类型/唯一请求ID随队列项保存；四类型交错入队/乱序swap removal/失败/未知来源隔离已测试；复用同一个Generic类的不同触发原因不进一步猜测 |
| 数量和权重改变球体大小 | 已实现成功生成的基础 DifficultyRating 总和；未纳入运行时倍率列表 |
| 房主同步客机 | 已实现标准 Actor 属性复制和客机本地绘制；双机/晚加入/丢包/切图待测 |
| 无 Mod 客机显示同款效果 | 不支持；使用任务允许的双方安装方案 |
| 有中心优先、多个中心分别标记 | 共享批次接口读取实际选中中心；同请求不同中心不合并；来源/请求不同也不合并；覆盖目录中的池、组、位置及回调路径 |
| 无中心的 Boss 直接召唤 | **未实现该来源**；不能把队列坐标回退说成覆盖了直接 SpawnActor |
| 自然潮小型单位排除 | 继续使用注册名单，small/critter优先；明确启用的脚本潮允许注册小型敌人，以支持蜂拥专项潮；环境生物仍排除 |
| 任意情况下的掠夺虫排除 | critter 排除及 Hoarder/Huuli 名称补充；改名/自定义派生类和其他 Mod 重分类需实测，尚无任意修改保证 |
| agent 清理 | 旧8个HostProject删除命令被工具以blocked by policy拒绝，具体策略组件未知；未删除，不绕过 |
| 发布前准备与分类说明 | 已提供；未上传，ReleaseReady=false；默认 Sandbox，最终审核决定 |
| 提前 1–5 秒精确预测及差异标记 | **未实现**；当前未来选点依赖随机数、玩家与导航；已实现实际选点后的延迟测量，无预测标记 |

## 实机排除测试

先以房主执行普通任务，等待游戏本身的自然潮，不使用手动刷怪代替自然调度。记录实际潮中心提示是否出现、是否稳定停在原点，以及关闭自然潮选项后是否消失。分别观察少量战士和较高成本敌人的中心球体大小。

然后分别触发：巢主哨卫、无畏蜂拥异虫、育母体死亡释放、两类大水母及岩痘幼虫、看守者召唤、科洛科新芽/果实、掠夺虫、两类蜂巢、酸液虫后、噬岩体孢子、普通巢穴/异骸洞穴掠痕虫、核心岩/核心侵扰、矿化爆发/三提石/除虫器事件、典狱长召唤。**预期这些来源不产生自然潮标记**。这张清单目前是待验收项目，未声称逐项实测。

使用Custom Difficulty把召唤物替换成普通战士或其他大怪，重复上述来源；自然调度链以外的请求不得标成自然。启用Salvage: mini-MULE ambush测试矿骡；再启用Generic、Excavation各阶段并设置不同文字，观察播报/古脂相关控制器重叠时的类型和中心。Generic被不同触发器复用时仍显示Generic，不能冒称已辨别具体触发原因。

联机：房主/客机安装同版，查看相同中心和规模；修改客机本地开关/文字/颜色，验证独立设置。观察未安装 Mod 的客机、晚加入、重连、往返空间站和新任务；无 Mod 客机不作为显示成功验收对象。最长会话超过 46 分钟时，还需检查日志采样停止与类重载不会影响显示；当前诊断调度有 600 次上限。

## 自然潮提前量诊断

DLL 在自己的安装目录旁自动写 `probe-*.jsonl`。开发者分析命令：

```powershell
node .\scripts\Analyze-NativeTiming.cjs '完整路径\probe-xxx.jsonl'
```

`timing`数组前五项仍是合格自然敌人样本数及选点/入队/交接时间，第六项现在累计所有已归因请求的过滤数。计时使用GetTickCount64，短间隔可能为0。无样本先排查capture_fault、auto_fault、hook_status、调用链和注册时序，不能解释成零延迟。

这些值测的是**游戏已经完成实际选点之后**，而非倒计时剩余五秒时的候选，也不是 GPU 首帧。在触发前提前调用原游戏选点/生成函数会消费 RNG 或改变刷怪，不符合观察型 Mod 的范围，因此未采用。即使观测到队列等待超过一秒，也不能推出每波有稳定一秒提前量。后续实验要同时测失败/取消请求和首个真正显示帧，才能决定是否适合加入明确区分的待生成标记。

## 后续源码工作

继续审计非EWC的独立召唤/机器事件组件及直接创建路径；为复用Generic但需要进一步区分的触发原因增加发起者记录。不要把当前目录中的36类型当成“任意敌人来源”，也不能凭空填写hook地址。自然链、每请求类型、未知隔离和多中心必须保持回归通过。
